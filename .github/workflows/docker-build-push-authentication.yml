# .github/workflows/docker-build-push-authentication.yml
name: Build and Push Docker Image (microservice-authentication)

on:
  push:
    paths:
      - 'microservice-authentication/**'
      - 'pom.xml'
      - '.github/workflows/docker-build-push-authentication.yml'
    branches:
      - prueba
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Run tests
        working-directory: ./microservice-authentication
        run: mvn test

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set image tag
        id: vars
        run: echo "tag=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./microservice-authentication/Dockerfile
          platforms: linux/amd64
          push: true
          tags: brunocaro/microservice-authentication:${{ steps.vars.outputs.tag }}

      - name: Update values.yaml
        run: |
          sed -i "s|brunocaro/microservice-authentication:.*|brunocaro/microservice-authentication:${{ steps.vars.outputs.tag }}|" microservices-helm-repo/charts/microservices-chart/values.yaml

      - name: Commit and push updated values.yaml
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add microservices-helm-repo/charts/microservices-chart/values.yaml
          git commit -m "Update authentication image to tag ${{ steps.vars.outputs.tag }}" || echo "No changes to commit"
          git remote set-url origin https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/BrunoC21/SGA_Gestion_Proyecto_Software.git
          git push origin prueba
