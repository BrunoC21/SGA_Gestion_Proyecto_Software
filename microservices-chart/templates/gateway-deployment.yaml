apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      initContainers:
        - name: wait-for-config
          image: busybox
          command: ['sh', '-c', 'until wget -qO- http://config-server:8888/actuator/health | grep UP; do echo waiting for config-server; sleep 5; done']
          # Timeout de 30 segundos por ejemplo
          terminationMessagePolicy: FallbackToLogsOnError
      containers:
        - name: gateway
          image: "{{ .Values.microservices.gateway.image }}"
          ports:
            - containerPort: {{ .Values.microservices.gateway.port }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.microservices.gateway.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  selector:
    app: gateway
  ports:
    - port: {{ .Values.microservices.gateway.port }}
      targetPort: {{ .Values.microservices.gateway.port }}
  type: ClusterIP
